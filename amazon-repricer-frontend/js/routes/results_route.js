// Generated by CoffeeScript 1.8.0
(function() {
  var ResultsRoute, utils;

  utils = require('../utils/utils');

  ResultsRoute = Ember.Route.extend({
    model: function() {
      return utils.get_url('http://' + utils.BACKEND_URL + '/result_set_names').then(function(ret) {
        var parsedResult;
        parsedResult = $.parseJSON(ret);
        return {
          resultSetNames: parsedResult,
          selectedResult: parsedResult.length > 0 ? parsedResult[0] : ''
        };
      });
    }
  });

  module.exports = ResultsRoute;

}).call(this);
